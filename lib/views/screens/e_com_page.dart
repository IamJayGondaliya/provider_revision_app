import 'package:flutter/material.dart';

class EcomPage extends StatelessWidget {
  const EcomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("E-com Page"),
      ),
    );
  }
}
